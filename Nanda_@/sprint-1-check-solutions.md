## 1: Find the maximum value
- **Time Complexity**: O(n)
- **Space Complexity**:O(1)
- **Explanationn**    : As in an fixed array it checks upto n numbers but we want to search one element. 

## 2: Check if a number is even or odd
- **Time Complexity**: O(1)
- **Space Complexity**: O(1)
- **Explanation**: Checks single  operation uses only single  variable.

## 3: Sum all elements in an array.
- **Time Complexity**: O(n)
- **Space Complexity**: O(1)
- **Explanation**: As the program takes more time and as amount of input doesnt depend on n .

## 4: Print numbers from 1 to n.
- **Time Complexity**: O(n)
- **Space Complexity**: O(1) 
- **Explanation**: As twe know that there will be size of n  and spCE complexity will be 1 due to it has a single variable.

## 5: Access the middle element of an array (assume even length).
- **Time Complexity**: O(n)
- **Space Complexity**: O(1)
- **Explanation**: The operation takes an constant time and since the array is fixed .


## 6:wap two integers without a temporary variable.
- **Time Complexity**: O(n)
- **Space Complexity**: O(1)
- **Explanation**: constant of operations and ONLY 2 variables .

##  7:Count the number of vowels in a string.
- **Time Complexity**: O(n)
- **Space Complexity**: O(1)
- **Explanation**: as the loop is n and only uses two variables .

## 8 :find length of a string without using stringlenght.
- **Time Complexity**: O(n) 
- **Space Complexity**: O(1)
- **Explanation**: as lenght of input incereses tc wil increase and does not allcate additional memory .

## 9:Check if a number is prime.
- **Time Complexity**: O(n)
- **Space Complexity**: O(1)
- **Explanation**: as it search to n and as usually sc is 1 .

## 10:Calculate factorial using a loop.
- **Time Complexity**: O(n)
- **Space Complexity**: O(1)
- **Explanation**: fixed amount of memory regardeless memory size .

## 11:Binary search on a sorted array.
- **Time Complexity**: O(n +logn)
- **Space Complexity**: O(1)
- **Explanation**: as have to read n elements in an array,the time to perform an binarycalculation and uses fixes size of array .

## 12:Merge two sorted arrays into one.
- **Time Complexity**: O(n + m)
- **Space Complexity**: O(1)
- **Explanation**:n is the size of the first array and m is the size of the second array and as the amount of memory used by the program does not depend on the sizes of the input arrays n and m.

## 13:Sort an array using bubble sort.
- **Time Complexity**: O(n^2)
- **Space Complexity**: O(1)
- **Explanation**:as the size of the input array n increases, the running time of the program will grow quadratically and the memory is fixed size array.






















